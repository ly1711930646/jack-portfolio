# 腾讯云 COS 配置
# 1) 复制本文件为 .env（.env 不会被提交）
# 2) 填入你自己的值
# 3) 在 COS 控制台把该存储桶设为「公有读」，并配置 CORS 允许你的网站域名（或 *）的 GET/PUT/POST
#
# 存储桶名称（格式：bucketname-APPID，例如 liyang-portfolio-1250000000）
VITE_COS_BUCKET=your-bucket-1250000000
# 地域，例如 ap-guangzhou / ap-shanghai
VITE_COS_REGION=ap-guangzhou
# 访问密钥（建议使用「子账号/临时密钥」仅授予该桶的上传与读取权限，降低泄露风险）
VITE_COS_SECRET_ID=your-secret-id
VITE_COS_SECRET_KEY=your-secret-key
# 可选：留空则自动拼接为 https://<bucket>.cos.<region>.myqcloud.com
VITE_COS_BASE_URL=
